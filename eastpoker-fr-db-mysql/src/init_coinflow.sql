use eastpoker;
create table if not exists coinflow0(
	id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid BIGINT ,
	chg BIGINT,
	type INT,
    time TIMESTAMP NOT NULL
);
create table if not exists coinflow1(
	id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid BIGINT ,
	chg BIGINT,
	type INT,
    time TIMESTAMP NOT NULL
);
create table if not exists coinflow2(
	id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid BIGINT ,
	chg BIGINT,
	type INT,
    time TIMESTAMP NOT NULL
);
create table if not exists coinflow3(
	id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid BIGINT ,
	chg BIGINT,
	type INT,
    time TIMESTAMP NOT NULL
);
create table if not exists coinflow4(
	id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid BIGINT ,
	chg BIGINT,
	type INT,
    time TIMESTAMP NOT NULL
);
create table if not exists coinflow5(
	id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid BIGINT ,
	chg BIGINT,
	type INT,
    time TIMESTAMP NOT NULL
);
create table if not exists coinflow6(
	id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid BIGINT ,
	chg BIGINT,
	type INT,
    time TIMESTAMP NOT NULL
);
create table if not exists coinflow7(
	id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid BIGINT ,
	chg BIGINT,
	type INT,
    time TIMESTAMP NOT NULL
);
create table if not exists coinflow8(
	id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid BIGINT ,
	chg BIGINT,
	type INT,
    time TIMESTAMP NOT NULL
);
create table if not exists coinflow9(
	id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	uid BIGINT ,
	chg BIGINT,
	type INT,
    time TIMESTAMP NOT NULL
);